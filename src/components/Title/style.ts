import styled from "styled-components";

const yellowCustom = "#ffc107";

export const Title = styled.h1`
  position: relative;
  letter-spacing: 2px;
  font-weight: 700;
  margin-bottom: 4px;
  padding-bottom: 4px;

  strong {
    color: ${yellowCustom};
  }
`;
